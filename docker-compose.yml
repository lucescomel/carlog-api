services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: carlog
      POSTGRES_USER: carlog
      POSTGRES_PASSWORD: carlog
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
